# Neethi AI — Environment Variables
# Copy this file to .env and fill in real values.
# NEVER commit .env to version control.

# ---------------------------------------------------------------------------
# Database (Supabase PostgreSQL)
# ---------------------------------------------------------------------------
DATABASE_URL=postgresql+asyncpg://postgres:YOUR_PASSWORD@db.YOUR_PROJECT_REF.supabase.co:5432/postgres

# ---------------------------------------------------------------------------
# Qdrant Vector Database (Qdrant Cloud free tier)
# ---------------------------------------------------------------------------
# Cluster name: neethi-legal
# After creating the cluster in cloud.qdrant.io, the URL format is:
#   https://<cluster-id>.<region>.aws.cloud.qdrant.io:6333
# Example:
#   https://a1b2c3d4-xxxx-xxxx-xxxx-xxxxxxxxxxxx.us-east4-0.gcp.cloud.qdrant.io:6333
# Get the exact URL and API key from: cloud.qdrant.io → Clusters → neethi-legal → Dashboard
QDRANT_URL=https://<cluster-id>.<region>.cloud.qdrant.io:6333
QDRANT_API_KEY=

# ---------------------------------------------------------------------------
# Embedding Model
# ---------------------------------------------------------------------------
BGE_M3_MODEL_PATH=BAAI/bge-m3

# ---------------------------------------------------------------------------
# Application
# ---------------------------------------------------------------------------
LOG_LEVEL=INFO
ENVIRONMENT=development

# ---------------------------------------------------------------------------
# LLM Providers (used in query pipeline — NOT in data pipeline)
# ---------------------------------------------------------------------------
GROQ_API_KEY=
MISTRAL_API_KEY=          # Fallback when Groq hits free-tier TPM limit — get from console.mistral.ai
DEEPSEEK_API_KEY=
ANTHROPIC_API_KEY=

# ---------------------------------------------------------------------------
# External Services
# ---------------------------------------------------------------------------
SARVAM_API_KEY=
SERP_API_KEY=
THESYS_API_KEY=

# ---------------------------------------------------------------------------
# Auth
# ---------------------------------------------------------------------------
JWT_SECRET_KEY=
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=60

# ---------------------------------------------------------------------------
# Cache (Upstash Redis)
# ---------------------------------------------------------------------------
# Local development:
#   REDIS_URL=redis://localhost:6379
#
# Upstash (production) — TLS required, token embedded in URL:
#   REDIS_URL=rediss://default:<UPSTASH_REDIS_REST_TOKEN>@<host>.upstash.io:6380
#
# The "rediss://" scheme (note the double-s) enables TLS automatically.
# Get the exact URL from: console.upstash.com → your database → Connect → redis-cli
REDIS_URL=redis://localhost:6379

# Optional: override TTLs (seconds).  Defaults: DIRECT=86400 (24 h), FULL=3600 (1 h)
# CACHE_TTL_DIRECT=86400
# CACHE_TTL_FULL=3600
