import type { Config } from "tailwindcss";

// Tailwind v4: theme tokens live in globals.css @theme {}
// This file is kept minimal — only options not supported in CSS are placed here.
const config: Config = {};

export default config;
